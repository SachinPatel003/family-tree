// import { createSlice } from "@reduxjs/toolkit";

// const initialState = {
//   isLoggedIn: false,
// };

// export const AuthenticationSlice = createSlice({
//   name: "authentication",
//   initialState,
//   reducers: {
//     updateAuthInfo: (state, action) => {
//       return action?.payload;
//     },
//   },
// });

// export const { updateAuthInfo } = AuthenticationSlice.actions;

// export const selectiIsLoggedIn = (state) => state.authentication.isLoggedIn;

// export const selectUserInfo = (state) => state.authentication;

// export default AuthenticationSlice.reducer;
